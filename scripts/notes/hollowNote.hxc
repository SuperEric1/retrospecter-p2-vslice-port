import funkin.play.notes.notekind.NoteKind;
import funkin.Paths;

class HollowNote extends NoteKind {
	public function new() {
		super('hollowNotes', 'Hollow Note', 'hollowNote', [], true); // id, description, styleId, params, noanim
	}

	/*public override function onNoteIncoming(event:NoteScriptEvent):Void {
		event.note.frames = Paths.getSparrowAtlas('notes/hollowNotes');
		event.note.animation.addByPrefix('purpleScroll', 'noteLeft');
		event.note.animation.addByPrefix('blueScroll', 'noteDown');
		event.note.animation.addByPrefix('greenScroll', 'noteUp');
		event.note.animation.addByPrefix('redScroll', 'noteRight');

		var hollowAnim:String;
		switch (event.note.noteData.data % 4) {
			case 0: hollowAnim = 'purpleScroll';
			case 1: hollowAnim = 'blueScroll';
			case 2: hollowAnim = 'greenScroll';
			case 3: hollowAnim = 'redScroll';
		}

		event.note.animation.play(hollowAnim);
	}*/
}
