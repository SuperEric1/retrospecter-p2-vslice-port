import funkin.play.notes.notekind.NoteKind;
import funkin.Paths;

class IzzyNote extends NoteKind {
	public function new() {
		super('izzyNote', 'Izzy Note', null, [], true); // id, description, styleId, params, noanim
		scoreable = false;
	}

	public override function onNoteIncoming(event:NoteScriptEvent):Void {
		if (!event.note.noteData.getMustHitNote()) return;

		event.note.x += -59;
		event.note.yOffset += -65;

		event.note.frames = Paths.getSparrowAtlas('notes/Pride_Note_Assets');
		event.note.animation.addByPrefix('purpleScroll', 'Left0');
		event.note.animation.addByPrefix('blueScroll', 'Down0');
		event.note.animation.addByPrefix('greenScroll', 'Up0');
		event.note.animation.addByPrefix('redScroll', 'Right0');

		var izzyAnim:String;
		switch (event.note.noteData.data % 4) {
			case 0: izzyAnim = 'purpleScroll';
			case 1: izzyAnim = 'blueScroll';
			case 2: izzyAnim = 'greenScroll';
			case 3: izzyAnim = 'redScroll';
		}

		event.note.active = true;
		event.note.animation.play(izzyAnim);
	}

	public override function onNoteMiss(event:NoteScriptEvent):Void {
		event.cancel();
		event.stopPropagation();
	}
}
