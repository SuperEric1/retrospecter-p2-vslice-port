import flixel.FlxObject;
import flixel.FlxSprite;
import flixel.FlxState;
import flixel.FlxG;
import flixel.text.FlxText;
import flixel.text.FlxTextBorderStyle;
import funkin.ui.mainmenu.MainMenuState;
import funkin.play.PlayState;
import funkin.Paths;
import funkin.util.ReflectUtil;
import funkin.util.Constants;
import funkin.modding.module.Module;
import funkin.modding.module.ModuleHandler;

class RetroSplashText extends Module
{
  public var retroV:String = '1.0';

  public function new()
  {
    super('RetroSplashText');
  }

  override function onUpdate(event:UpdateScriptEvent)
  {
	  super.onUpdate(event);

      if (ReflectUtil.getClassNameOf(FlxG.state) == "funkin.ui.mainmenu.MainMenuState")
      {
          FlxG.state.leftWatermarkText.font = Paths.font('Bebas-Regular.ttf');
          FlxG.state.leftWatermarkText.size = 20;
          FlxG.state.leftWatermarkText.text = "Vs. RetroSpecter - Part Two: Infernadise (V-SLICE)\nFan Port - v" + retroV;
          FlxG.state.leftWatermarkText.y = FlxG.height - FlxG.state.leftWatermarkText.fieldHeight;
      }
  }
}
